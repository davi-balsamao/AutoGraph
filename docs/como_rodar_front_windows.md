# Como rodar e testar o frontend no navegador (Windows)

Abra o terminal e entre na pasta do frontend:

```bash
cd frontend
```
Baixe as dependências do projeto:

```bash
flutter pub get
```

Rode o aplicativo (o parâmetro -d chrome força a rodar no Google Chrome):

```bash
flutter run -d chrome
```