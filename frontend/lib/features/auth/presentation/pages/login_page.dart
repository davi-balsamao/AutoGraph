import 'package:flutter/material.dart';

/// Tela de Login — ponto de entrada do módulo auth.
/// Implementação completa será realizada no Card de autenticação (Milestone 3).
class LoginPage extends StatelessWidget {
  const LoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Login — Em desenvolvimento'),
      ),
    );
  }
}
