import 'package:flutter/material.dart';

/// Tela de rastreio da OS do cliente.
/// Implementação completa será realizada no Card da área do cliente (Milestone 3).
class OsClientePage extends StatelessWidget {
  const OsClientePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Minhas Ordens de Serviço — Em desenvolvimento'),
      ),
    );
  }
}
