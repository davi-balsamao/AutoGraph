import 'package:flutter/material.dart';

/// Dashboard do gerente — Kanban de OS e painel de controle.
/// Implementação completa será realizada no Card do painel admin (Milestone 3).
class AdminDashboardPage extends StatelessWidget {
  const AdminDashboardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Painel do Gerente — Em desenvolvimento'),
      ),
    );
  }
}
